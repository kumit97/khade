{
  "name": "khade",
  "version": "0.1.0",
  "private": true,
  "description": "KHADE — beauty & wellness marketplace (monorepo)",
  "workspaces": [
    "packages/*",
    "apps/admin-dashboard",
    "apps/business-app"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:reset": "supabase db reset",
    "db:diff": "supabase db diff"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "packageManager": "npm@10.9.7",
  "engines": {
    "node": ">=20"
  }
}
