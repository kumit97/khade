# Live project "khadeapp". Copy the anon/publishable + service-role keys from
# the Supabase dashboard (Project Settings → API). Do NOT commit real values.
NEXT_PUBLIC_SUPABASE_URL=https://qfxdatptvypebnntzvhh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
# Server-only. Required for admin actions that must bypass RLS responsibly
# (verification queue, platform reports). Never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
